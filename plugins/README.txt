Put plugin files here.

GUITester searches plugin files in here and sub-folders recursively.
  - .class file: plugin class files are loaded with default package. (Plugin class should have no package names.) 
  - .jar file: top-level plugin class files inside the jar file are loaded.
   